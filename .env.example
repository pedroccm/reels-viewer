# Password to unlock editing (add tags / hide videos). Set this in Netlify env too.
EDIT_PASSWORD=change-me

# Only needed by `npm run push-manifest` (uploads the local manifest to Netlify Blobs):
NETLIFY_SITE_ID=
NETLIFY_AUTH_TOKEN=
